# SOP_WEEKLY_CHECKLIST.md

1. Run `uid_check.py` to validate current markdown UID tags.
2. Run `uid_indexer.py` to regenerate UID master table.
3. Review folders like `PD-GG`, `SB-ML`, `MT-PR`, and `NR-TH` for growth.
4. Update or annotate index.md files in active folders.
5. Freeze the current snapshot with `vault_freeze.py` if needed.
